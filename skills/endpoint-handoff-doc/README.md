# Endpoint Handoff Doc

This skill generates frontend-focused endpoint handoff documentation by reading backend routes, controllers, services, and OpenAPI definitions.

## Recommended Scope

`Project skill`

Recommendation: keep this with the project whose backend it documents. The workflow depends on that codebase's routing, controller, service, and API contract structure.

Invoke it with `$endpoint-handoff-doc`.
