# Semantic Commits

This skill creates git commits using Conventional Commit semantics and stages either the user-selected files or the full working tree as appropriate.

## Recommended Scope

`Global/personal skill`

Recommendation: install this globally because semantic commit formatting and commit hygiene are cross-project workflows, not repository-specific logic.

Invoke it with `$semantic-commits`.
